import { PUBLIC_TRIPS_HEADER } from "assets";
import MenuHeader from "components/menuHeader";
import React from "react";

export default function Trips() {
	return (
		<div>
			<MenuHeader img={PUBLIC_TRIPS_HEADER} />
		</div>
	);
}
